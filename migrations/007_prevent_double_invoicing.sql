-- =====================================================================
-- جلوگیری از صدور دوباره‌ی صورتحساب برای یک بیمه‌نامه‌ی یکسان.
-- برنامه‌ی قدیمی ویندوزی (database.py/main.py) هر بیمه‌نامه‌ای که وارد یک
-- صورتحساب می‌شد را is_invoiced=True می‌کرد و دفعه‌ی بعد از فهرست «صدورنشده‌ها»
-- حذفش می‌کرد؛ این منطق در سایت وجود نداشت و یک پرونده می‌توانست به اشتباه
-- دو بار (مثلاً هم در یک صورتحساب تفکیکیِ یک شرکت، هم بعداً در یک صورتحساب
-- تجمیعیِ کل دوره) صورتحساب بخورد.
-- فقط ستون اضافه می‌کند - هیچ داده‌ای تغییر/حذف نمی‌شود.
-- =====================================================================
ALTER TABLE `policy_cases`
  ADD COLUMN `is_invoiced` TINYINT(1) NOT NULL DEFAULT 0 AFTER `status`,
  ADD COLUMN `invoiced_invoice_id` BIGINT DEFAULT NULL AFTER `is_invoiced`;
