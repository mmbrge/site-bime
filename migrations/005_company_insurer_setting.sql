-- =====================================================================
-- اینکه یک شرکت مجاز است چه بیمه‌گری/بیمه‌گرهایی را درخواست کند
-- (پاسارگاد، ایران، یا هردو) - در تنظیمات شرکت مشخص می‌شود و پنل ثبت‌کننده
-- فقط همان گزینه(ها) را نشان می‌دهد.
-- =====================================================================
ALTER TABLE `companies`
  ADD COLUMN `allowed_insurers` ENUM('PASARGAD','IRAN','BOTH') NOT NULL DEFAULT 'BOTH' AFTER `bale_group_chat_id`;
